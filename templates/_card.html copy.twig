<div class="m-3 p-3 border rounded bg-white row col-xl-3 col-lg-4 col-12">

    <div class="d-flex flex-column">
        <div class="h-3">
            <div class="d-flex gap-3 justify-content-between flex-wrap">
                <h3>
                    {{ (job.recruiter | default('job_recruiter') ) }}
                </h3>
                <h6>

                    <span class="badge text-bg-secondary">
                    {{ job.action_name  | default('job_action_name' ) }}
                    il y a
                    {{ job.delai  | default('job_delai') }}j
                    </span>
                </h6>
                <h6>

                    <span class="badge text-bg-secondary">
                        {{ job.note_count | default('job_note_count') }}
                        postits
                    </span>
                </h6>

      
            </div>
            <h4>
                {{ job.title  | default('job_title') }}
            </h4>
               
        </div>

        <h5>
           
           j
        </h5>
    </div>
    <hr/>

    <div>
        <p class="text-end flex-grow-1">

            <a href="{{ path('app_job_tracking',  {'id':job.id | default('job_id')}) }}">
                Visualiser
            </a>
        </p>
    </div>
</div>
